# 一套系统工具库

提供`cli`和`js api`两种调用方式，以`esm`包分发

提供`ts`类型支持

需要`node24`及以上

用的第三方包会全部打包，使用的时候不需要安装额外依赖

## 场景覆盖(wip)

### 1. 解压缩

覆盖zip 7z gzip rar及带密码的支持，cli提供路径、密码及压缩类型输入

js api提供路径、uint8array、压缩包类型、密码输入参数
